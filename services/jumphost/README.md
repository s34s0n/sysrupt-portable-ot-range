# Jump Host

DMZ jump host providing SSH access to OT network segments.

## Setup

```bash
sudo ./setup.sh
```

## Credentials
- maintenance / maint2024!

## Intel
- `.bash_history` contains previous admin commands revealing OT device IPs and tools
- `hosts` file maps device names to IPs
